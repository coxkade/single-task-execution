{
    "tasks": [
        {
            "type": "shell",
            "label": "build all",
            "group": "build",
            "command": "make clean && make -j$(nproc) all && make semaphore-clean",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ],
    "version": "2.0.0"
}